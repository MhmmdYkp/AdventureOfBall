using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CheckBallColor : MonoBehaviour
{
    /*
    public Material m1, m2, m3,m4,m5;
    public MeshRenderer ball;
    public Dropdown dropDown;

    void Start()
    {
        dropDown.value = PlayerPrefs.GetInt("BallColor");
        ball = transform.GetComponent<MeshRenderer>();

        if (PlayerPrefs.GetInt("BallColor") == 0)
        {
            ball.material = m1;
        }
        else if (PlayerPrefs.GetInt("BallColor") == 1)
        {
            ball.material = m2;
        }
        else if (PlayerPrefs.GetInt("BallColor") == 2)
        {
            ball.material = m3;
        }
        else if (PlayerPrefs.GetInt("BallColor") == 3)
        {
            ball.material = m4;
        }
        else if (PlayerPrefs.GetInt("BallColor") == 4)
        {
            ball.material = m5;
        }
    }

    public void UpdateColor()
    {
        GetComponent<Dropdown>().value = PlayerPrefs.GetInt("BallColor");
    }
    */
}
